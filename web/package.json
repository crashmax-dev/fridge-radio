{
  "name": "web",
  "description": "web",
  "version": "1.0.0",
  "scripts": {
    "dev": "parcel src/index.html --no-cache --host 127.0.0.1 --port 8080",
    "build": "parcel build src/index.html --public-url ./",
    "lint": "eslint src --ext .ts",
    "lint:fix": "eslint src --ext .ts --fix"
  },
  "staticFiles": {
    "staticPath": "static"
  },
  "devDependencies": {
    "parcel": "^2.3.2",
    "parcel-reporter-clean-dist": "^1.0.4",
    "parcel-reporter-static-files-copy": "^1.3.4",
    "typescript": "^4.6.2"
  },
  "dependencies": {
    "redom": "^3.27.1"
  }
}
